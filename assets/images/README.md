# Image notes

This GitHub Pages site uses optimized, lazy-loaded Unsplash image URLs so the repository stays lightweight. Replace the remote image URLs with coach-owned images in this folder before launch if available.
